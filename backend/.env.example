# Example environment variables for Tagline Backend

BACKEND_API_KEY=changeme
LOG_LEVEL=INFO
LOG_FORMAT=json  # "json" for structured logs or "human" for readable output

# Development Authentication Bypass
AUTH_BYPASS_EMAILS=dev@example.com,test@example.com
AUTH_BYPASS_ENABLED=true

# JWT Authentication
JWT_SECRET=change-this-to-a-secure-random-string
JWT_ALGORITHM=HS256

# Stytch Authentication
STYTCH_PROJECT_ID=project-test-xxxxx
STYTCH_SECRET=secret-test-xxxxx
STYTCH_ENV=test  # 'test' or 'live'

# Administrator Access
# ADMINISTRATOR_EMAIL=admin@example.com  # This email always has access

# Example PostgreSQL connection string
DATABASE_URL=postgresql+psycopg2://tagline:tagline@postgres:5432/tagline
UNIT_TEST_DATABASE_URL=postgresql+psycopg2://tagline:tagline@localhost:5432/tagline_test

# Storage Provider
STORAGE_PROVIDER=filesystem

# Filesystem Storage Provider
FILESYSTEM_ROOT_PATH=/app/media

# Dropbox Storage Provider
DROPBOX_APP_KEY=your-app-key
DROPBOX_APP_SECRET=your-app-secret
DROPBOX_REFRESH_TOKEN=your-refresh-token
# DROPBOX_ROOT_PATH=/your-root-folder  # Optional: defaults to "/" (Dropbox root)

# Storage Path Filtering
# PREFIXES_TO_IGNORE='["/Archive", "/Personal Files", "/Test Data"]'  # JSON array of prefixes to exclude
